<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:padding="10dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="5dp"
    tools:context=".MainActivityAllBook"
    android:id="@+id/cardAreadyRead">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/light_gray_card"
        >

        <ImageView
            android:layout_width="120dp"
            android:layout_height="150dp"
            android:id="@+id/bookPhotoAready"
            android:layout_centerHorizontal="true"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"

            />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/infoBoxAready"
            android:textAlignment="center"
            android:gravity="center"
            android:textColor="@color/black"
            android:background="@color/btn_light_green"
            android:text="This ia an information box"
            android:layout_below="@+id/bookPhotoAready"
            android:layout_centerHorizontal="true"
            android:padding="3dp"
            />

    </RelativeLayout>
</androidx.cardview.widget.CardView>